# Cloudflare Pages config — makes Git-connected auto-deploy zero-config.
# The site is a static build; Cloudflare serves everything in ./deploy as-is.
name = "vparchitect-website"
compatibility_date = "2025-01-01"
pages_build_output_dir = "deploy"
